language: ruby
cache: bundler
rvm:
- 2.1
before_script: bundle update
script: bundle exec jekyll build -s site -d _site --trace && bundle exec htmlproof ./_site
branches:
  only:
  - master
  - /post-(.*)/
